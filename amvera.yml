meta:
  environment: node
  toolchain:
    name: npm
    version: 20
build:
  requirements: package.json
  command: export ASTRO_ADAPTER=node && npm run build
run:
  command: npm run start
  persistence:
    - mountPoint: /data