meta:
  environment: node
  toolchain: npm
  version: 20

build:
  command: npm install && npm run build
  artifacts:
    - key: "**"
      value: "/"

run:
  scriptName: dist/server/entry.mjs
  containerPort: 4321
  persistenceMount: /data