---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import ProductCard from '../components/ProductCard.astro';
import { getCollection, getEntry } from 'astro:content';

// 1. DATA FETCHING
const landingEntry = await getEntry('landing', 'home');
const data = landingEntry?.data || {};

const FALLBACK_HERO = "/images/hero-minimal.jpg";
const FALLBACK_WORKSHOP = "/images/workshop-process.jpg";

const HERO = {
    image: data.heroImage || FALLBACK_HERO,
    titleLine1: data.heroTitleLine1 || "Глина",
    titleAccent: data.heroTitleAccent || "хранит",
    titleLine2: data.heroTitleLine2 || "тепло.",
    description: data.heroDescription || "Вещи, созданные в тишине. С несовершенствами, которые делают их живыми."
};

const WORKSHOP = {
    image: data.workshopImage || FALLBACK_WORKSHOP,
    title: data.workshopTitle || "Красота в несовершенстве.",
    text: data.workshopText || "В блоге мы делимся процессом: как бесформенный кусок глины обретает характер."
};

const allProducts = await getCollection('products');
const recentProducts = allProducts.slice(0, 8); 

const allCategories = await getCollection('categories');
const categoryMap = allCategories.reduce((acc, cat) => {
    acc[cat.id] = cat.data.title;
    return acc;
}, {} as Record<string, string>);
---

<Layout title="Главная" hideHeader={true}>
  
  <!-- HEADER (Absolute) -->
  <nav class="absolute top-0 left-0 w-full z-50 px-6 py-8 md:px-12 flex justify-between items-start mix-blend-difference text-[#F9F8F6]">
      <a href="/" class="group flex flex-col gap-0.5">
          <span class="font-serif text-3xl md:text-4xl leading-none transition-colors block">Hyle.</span>
      </a>
      
      <div class="hidden md:flex gap-12 font-mono text-[11px] tracking-[0.2em] uppercase pt-2">
          <a href="/catalog" class="hover:opacity-70 transition-opacity">каталог</a>
          <a href="/about" class="hover:opacity-70 transition-opacity">о мастере</a>
          <a href="/blog" class="hover:opacity-70 transition-opacity">блог</a>
      </div>

      <button class="md:hidden pt-2 text-[10px] uppercase tracking-widest border-b border-current pb-0.5">
          меню
      </button>
  </nav>

  <!-- HERO SECTION: GALLERY STYLE -->
  <section class="relative min-h-screen w-full flex items-center justify-center overflow-hidden bg-[#1a1918]">
    
    <!-- 1. BACKGROUND TYPOGRAPHY (Architectural Anchor) -->
    <!-- Огромная надпись HYLE на фоне, еле заметная -->
    <div class="absolute inset-0 flex items-center justify-center z-0 select-none overflow-hidden pointer-events-none">
        <span class="font-serif text-[40vw] text-[#F9F8F6] opacity-[0.03] leading-none tracking-tighter translate-y-10">
            Hyle
        </span>
    </div>

    <!-- 2. MAIN CONTENT GRID -->
    <div class="container-main relative z-10 w-full h-full grid grid-cols-1 lg:grid-cols-12 items-center gap-0 lg:gap-12 pt-20 lg:pt-0">
        
        <!-- IMAGE BLOCK (Dominant, Right/Center) -->
        <div class="lg:col-span-7 lg:col-start-6 relative order-1 lg:order-2">
            <!-- Image Wrapper -->
            <div class="relative w-full aspect-[3/4] md:aspect-[4/5] lg:aspect-[16/10] overflow-hidden shadow-2xl">
                 <img 
                    src={HERO.image} 
                    alt="Hero Ceramics" 
                    class="w-full h-full object-cover opacity-90 hover:scale-105 transition-transform duration-[2s] ease-out"
                    onerror={`this.onerror=null; this.src='${FALLBACK_HERO}';`}
                />
                <!-- Texture Overlay -->
                <div class="absolute inset-0 bg-noise opacity-20 mix-blend-overlay pointer-events-none"></div>
                
                <!-- Technical Badge (Top Right of Image) -->
                <div class="absolute top-0 right-0 bg-[#F9F8F6] text-[#1a1918] p-4 hidden md:block">
                    <div class="flex flex-col font-mono text-[9px] uppercase tracking-widest leading-relaxed">
                        <span>Col. 2026</span>
                        <span>Handmade</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- TEXT BLOCK (Left, Overlapping) -->
        <!-- Negative margin-right to pull text over image -->
        <div class="lg:col-span-6 lg:col-start-1 lg:-mr-24 relative z-20 order-2 lg:order-1 mt-8 lg:mt-0 pointer-events-none">
            <div class="animate-appear pointer-events-auto">
                
                <h1 class="font-serif text-6xl md:text-8xl lg:text-[7.5rem] leading-[0.85] text-[#F9F8F6] mb-8 tracking-tight mix-blend-difference">
                    {HERO.titleLine1} <br/>
                    <span class="italic font-light text-[#B35A32] ml-16 md:ml-32 block">
                        {HERO.titleAccent}
                    </span>
                    {HERO.titleLine2}
                </h1>

                <div class="flex flex-col md:flex-row items-start gap-8 lg:pl-2">
                    <p class="font-sans font-light text-sm md:text-base text-[#F9F8F6]/70 max-w-xs leading-relaxed border-l border-white/20 pl-4">
                        {HERO.description}
                    </p>
                    
                    <a href="/catalog" class="group flex items-center gap-3">
                        <div class="w-12 h-12 rounded-full border border-white/20 flex items-center justify-center text-[#F9F8F6] group-hover:bg-[#B35A32] group-hover:border-[#B35A32] transition-all duration-300">
                            <span class="text-xl relative top-[-2px] group-hover:translate-x-0.5 transition-transform">→</span>
                        </div>
                        <span class="font-mono text-xs uppercase tracking-widest text-[#F9F8F6]/80 group-hover:text-white transition-colors">
                            Каталог
                        </span>
                    </a>
                </div>
            </div>
        </div>

        <!-- META DATA (Bottom Left Absolute) -->
        <div class="absolute bottom-8 left-4 hidden lg:flex flex-col gap-1 font-mono text-[9px] uppercase tracking-widest text-[#F9F8F6]/30">
            <span>Lat: 55.7558</span>
            <span>Lon: 37.6173</span>
            <span>Mat: Clay / Fire</span>
        </div>

    </div>
  </section>

  <!-- SECTION 2: CATALOG (Light Background starts here) -->
  <section class="py-24 md:py-32 bg-[#FDFCF8] relative z-20 overflow-hidden rounded-t-[40px] -mt-12 shadow-[0_-20px_60px_rgba(0,0,0,0.1)]">
    <div class="container-main">
        
        <!-- Header -->
        <div class="flex flex-col md:flex-row justify-between items-end mb-16 gap-6">
            <div class="max-w-lg">
                <span class="font-mono text-[10px] uppercase tracking-[0.2em] text-hyle-text/40 block mb-2">Избранное</span>
                <h2 class="font-serif text-4xl md:text-6xl text-hyle-text">
                    Новое из печи
                </h2>
            </div>
            
            <div class="flex items-center gap-3 text-hyle-text/30">
                <span class="font-mono text-[10px] uppercase tracking-widest">Листайте</span>
                <span class="text-lg">→</span>
            </div>
        </div>

        <!-- Horizontal Scroll -->
        <div 
            id="horizontal-showcase" 
            class="flex gap-8 overflow-x-auto pb-20 px-5 -mx-5 md:px-0 md:mx-0 snap-x md:snap-none cursor-ew-resize scrollbar-hide overscroll-x-contain"
        >
            {recentProducts.map((product, idx) => {
                const catId = (product.data.category && typeof product.data.category === 'object') 
                    ? product.data.category.id 
                    : product.data.category;
                
                const offsetClass = idx % 2 !== 0 ? "mt-12" : "";

                return (
                    <div class={`shrink-0 w-[85vw] md:w-[420px] snap-center transition-transform duration-700 hover:-translate-y-2 ${offsetClass}`}>
                         <ProductCard 
                            product={product} 
                            categoryName={catId ? categoryMap[catId] : ''} 
                        />
                    </div>
                );
            })}
            
            <div class="shrink-0 w-[200px] flex items-center justify-center snap-center md:snap-align-none">
                 <a href="/catalog" class="group flex flex-col items-center gap-4 text-hyle-text/40 hover:text-hyle-clay transition-colors">
                    <div class="w-20 h-20 rounded-full border border-current flex items-center justify-center group-hover:scale-110 transition-transform">
                        <span class="text-2xl">→</span>
                    </div>
                    <span class="font-mono text-xs uppercase tracking-widest">Все изделия</span>
                 </a>
            </div>
            <div class="w-12 shrink-0"></div>
        </div>

    </div>
  </section>

  <!-- UNIFIED DARK SECTION: PHILOSOPHY + FOOTER -->
  <div class="bg-[#1F1E1C] text-[#F9F8F6] relative overflow-hidden">
      <div class="absolute inset-0 opacity-10 bg-[url('https://www.transparenttextures.com/patterns/stardust.png')] mix-blend-overlay pointer-events-none"></div>
      
      <section class="py-24 md:py-40 relative z-10">
          <div class="container-main grid grid-cols-1 lg:grid-cols-2 gap-16 lg:gap-32 items-center">
              <div class="relative order-2 lg:order-1 pl-4 lg:pl-0">
                  <div class="group relative z-10 aspect-[4/5] overflow-hidden rounded-sm w-[90%] shadow-2xl bg-[#2C2B29]">
                      <img 
                        src={WORKSHOP.image} 
                        alt="Процесс" 
                        class="w-full h-full object-cover grayscale opacity-90 group-hover:grayscale-0 group-hover:opacity-100 transition-all duration-[1.5s] ease-out"
                        onerror={`this.onerror=null; this.src='${FALLBACK_WORKSHOP}';`}
                      />
                  </div>
              </div>
              <div class="order-1 lg:order-2">
                  <span class="font-mono text-[10px] uppercase tracking-[0.2em] text-hyle-clay mb-6 block">Философия</span>
                  <h2 class="font-serif text-4xl md:text-6xl mb-8 leading-tight">
                      {WORKSHOP.title}
                  </h2>
                  <div class="space-y-6 text-white/50 font-light text-lg leading-relaxed max-w-md border-l border-white/10 pl-6">
                      <p>{WORKSHOP.text}</p>
                  </div>
                  <div class="mt-12">
                      <a href="/about" class="font-mono text-xs uppercase tracking-widest border-b border-white/30 pb-1 hover:border-hyle-clay hover:text-hyle-clay transition-colors">
                          О мастере
                      </a>
                  </div>
              </div>
          </div>
      </section>

      <footer class="pb-20 pt-0 relative z-10">
          <div class="container-main flex flex-col items-center text-center">
              <div class="mb-10 opacity-10 w-full h-px bg-white"></div>
              <div class="flex flex-col items-center gap-6">
                  <span class="font-serif text-3xl text-[#F9F8F6]">Hyle.</span>
                  <nav class="flex gap-8 md:gap-12 font-mono text-[11px] text-white/60 uppercase tracking-widest">
                      <a href="/catalog" class="hover:text-hyle-clay transition-colors">каталог</a>
                      <a href="/about" class="hover:text-hyle-clay transition-colors">о мастере</a>
                      <a href="/blog" class="hover:text-hyle-clay transition-colors">блог</a>
                  </nav>
                  <div class="flex gap-6 mt-2">
                      <a href="#" class="w-10 h-10 rounded-full border border-white/10 flex items-center justify-center hover:border-hyle-clay hover:text-hyle-clay transition-colors text-white/40"><span class="sr-only">Telegram</span>Tg</a>
                  </div>
                  <p class="mt-8 text-[10px] text-white/20 font-mono tracking-widest uppercase">
                      © 2026 Москва
                  </p>
              </div>
          </div>
      </footer>
  </div>
</Layout>

<script>
    const showcase = document.getElementById('horizontal-showcase');
    let target = 0;
    let current = 0;
    let isAnimating = false;

    if (showcase) {
        target = showcase.scrollLeft;
        current = showcase.scrollLeft;

        showcase.addEventListener('wheel', (evt) => {
            if (window.matchMedia("(min-width: 768px)").matches) {
                const maxScroll = showcase.scrollWidth - showcase.clientWidth;
                const canScrollRight = target < maxScroll - 2;
                const canScrollLeft = target > 2;

                if ((evt.deltaY > 0 && canScrollRight) || (evt.deltaY < 0 && canScrollLeft)) {
                    evt.preventDefault();
                    target += evt.deltaY * 2.5;
                    target = Math.max(0, Math.min(target, maxScroll));

                    if (!isAnimating) {
                        isAnimating = true;
                        requestAnimationFrame(smoothScrollLoop);
                    }
                }
            }
        }, { passive: false });
    }

    function smoothScrollLoop() {
        if (!showcase) return;
        current += (target - current) * 0.08;
        showcase.scrollLeft = current;

        if (Math.abs(target - current) > 0.5) {
            requestAnimationFrame(smoothScrollLoop);
        } else {
            isAnimating = false;
            showcase.scrollLeft = target;
        }
    }
</script>