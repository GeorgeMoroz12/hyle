meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  # Amvera автоматически запустит npm install и npm run build
  skip: false

run:
  # Запуск собранного сервера Astro
  command: node ./dist/server/entry.mjs
  containerPort: 80