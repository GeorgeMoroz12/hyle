---
// src/components/ProductCard.astro
import type { CollectionEntry } from 'astro:content';
import { getEntry } from 'astro:content';

interface Props {
  product: CollectionEntry<'products'>;
  categoryName?: string;
}

const { product, categoryName } = Astro.props;
const { title, price, images, status, tags } = product.data;

const mainImage = images && images.length > 0 ? images[0] : '/images/placeholder.jpg';
const formattedPrice = price > 0 ? `${price.toLocaleString('ru-RU')} ₽` : 'По запросу';

const s = status?.toLowerCase() || '';
const isSold = s === 'sold' || s === 'продано';
const isReserved = s === 'reserved' || s === 'резерв';
const productUrl = `/products/${product.slug}`;

// --- ЛОГИКА РЕЗОЛВА ТЕГА (1 штука) ---
let visibleTag = null;
const rawTags = Array.isArray(tags) ? tags : [];

if (rawTags.length > 0) {
    // Берем первый тег для отображения
    const firstTag = rawTags[0];
    
    // Пытаемся зарезолвить
    try {
        let tagData = null;
        let tagId = null;

        // Вариант 1: Ссылка
        if (typeof firstTag === 'object' && firstTag !== null) {
             tagId = firstTag.id || firstTag.slug;
             tagData = await getEntry(firstTag); // getEntry(ref)
        } 
        // Вариант 2: Строка
        else if (typeof firstTag === 'string') {
             tagId = firstTag;
             tagData = await getEntry('tags', firstTag);
        }

        if (tagData && tagData.data) {
            visibleTag = {
                id: tagId,
                title: tagData.data.title // ВАЖНО: поле title из справочника
            };
        } else {
             // Fallback: просто ID с заглавной буквы
             visibleTag = {
                id: typeof firstTag === 'string' ? firstTag : tagId,
                title: (typeof firstTag === 'string' ? firstTag : tagId)
             };
             if (visibleTag.title) {
                 visibleTag.title = visibleTag.title.charAt(0).toUpperCase() + visibleTag.title.slice(1);
             }
        }
    } catch (e) {
        // Fail silently
    }
}
---

<div class="group relative w-full flex flex-col">
  
  <a href={productUrl} class="block relative overflow-hidden aspect-square bg-neutral-100 rounded-sm mb-3 cursor-pointer">
    {(isSold || isReserved) && (
      <div class="absolute top-2 left-2 z-30">
        <span class="text-[9px] font-sans font-medium uppercase tracking-[0.2em] text-hyle-text px-2 py-1 bg-white/90 backdrop-blur-md border border-hyle-text/10 shadow-sm">
           {isSold ? 'Продано' : 'Резерв'}
        </span>
      </div>
    )}
    
    <img 
      src={mainImage} 
      alt={title} 
      loading="lazy"
      class={`w-full h-full object-cover object-center transition-transform duration-[1.2s] ease-out-expo group-hover:scale-105 z-10 relative ${isSold ? 'grayscale opacity-90' : ''}`}
    />
  </a>

  <div class="flex justify-between items-start px-1 mt-auto">
    <div class="pr-2 flex flex-col items-start w-full min-w-0 relative">
        
        {categoryName && (
            <span class="text-[9px] uppercase tracking-[0.2em] text-hyle-text/40 mb-1 block transition-colors group-hover:text-hyle-clay/70">
                {categoryName}
            </span>
        )}

        <a href={productUrl} class="block mb-0.5 w-full">
            <h3 class="font-serif text-base md:text-lg leading-normal text-hyle-text transition-colors duration-300 group-hover:text-hyle-clay line-clamp-2 pb-1.5 -mb-1.5">
              {title}
            </h3>
        </a>
        
        {visibleTag && (
            <a 
                href={`/catalog?tag=${visibleTag.id}`}
                class="inline-block text-[11px] md:text-[13px] font-hand text-hyle-text/60 -rotate-1 mt-1 hover:text-hyle-clay transition-all z-10 relative"
            >
                #{visibleTag.title}
            </a>
        )}
    </div>
    
    <div class="flex flex-col items-end shrink-0 pl-1 pt-1">
        <span class="font-hand text-lg md:text-2xl text-hyle-clay font-medium whitespace-nowrap -rotate-2 group-hover:rotate-0 transition-transform origin-center">
          {formattedPrice}
        </span>
    </div>
  </div>
</div>